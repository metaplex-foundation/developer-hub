---
title: validate
metaTitle: validate | Sugar
description: validate 命令。
---

`validate` 命令用于检查资产文件夹中的所有文件是否格式正确：

```
sugar validate
```

如果您的资产位于非默认 `assets` 的文件夹中，可以指定文件夹名称：

```
sugar validate <ASSETS FOLDER>
```

{% callout %}

在上传之前验证您的资产非常重要，以避免重复上传过程。

{% /callout %}
