/**
 * Example: mint-tokens
 *
 * 
 *
 * This file is auto-generated by scripts/build-examples.js
 * Edit the native .js and .rs files, then run: node scripts/build-examples.js
 */

const kitSections = {
  "imports": "import {\n  getMintV1InstructionAsync,\n  TokenStandard,\n} from '@metaplex-foundation/mpl-token-metadata-kit';",
  "setup": "// Assuming rpc, rpcSubscriptions, and sendAndConfirm are set up\n// mint and authority from createV1\n\nconst mintAddress = mint.address; // From the created mint\nconst tokenOwner = authority.address; // Wallet to receive the token",
  "main": "// Mint the NFT token\nconst mintIx = await getMintV1InstructionAsync({\n  mint: mintAddress,\n  authority,\n  payer: authority,\n  amount: 1,\n  tokenOwner,\n  tokenStandard: TokenStandard.NonFungible,\n});\n\nawait sendAndConfirm({\n  instructions: [mintIx],\n  payer: authority,\n});",
  "output": "console.log('Minted NFT to:', tokenOwner);",
  "full": "// [IMPORTS]\nimport {\n  getMintV1InstructionAsync,\n  TokenStandard,\n} from '@metaplex-foundation/mpl-token-metadata-kit';\n// [/IMPORTS]\n\n// [SETUP]\n// Assuming rpc, rpcSubscriptions, and sendAndConfirm are set up\n// mint and authority from createV1\n\nconst mintAddress = mint.address; // From the created mint\nconst tokenOwner = authority.address; // Wallet to receive the token\n// [/SETUP]\n\n// [MAIN]\n// Mint the NFT token\nconst mintIx = await getMintV1InstructionAsync({\n  mint: mintAddress,\n  authority,\n  payer: authority,\n  amount: 1,\n  tokenOwner,\n  tokenStandard: TokenStandard.NonFungible,\n});\n\nawait sendAndConfirm({\n  instructions: [mintIx],\n  payer: authority,\n});\n// [/MAIN]\n\n// [OUTPUT]\nconsole.log('Minted NFT to:', tokenOwner);\n// [/OUTPUT]\n"
}

const umiSections = {
  "imports": "import { mintV1, TokenStandard } from '@metaplex-foundation/mpl-token-metadata';",
  "setup": "// Assuming umi is set up with mplTokenMetadata plugin\n// mint from createV1\n\nconst mintPublicKey = mint.publicKey; // From the created mint\nconst tokenOwner = umi.identity.publicKey; // Wallet to receive the token",
  "main": "// Mint the NFT token\nawait mintV1(umi, {\n  mint: mintPublicKey,\n  authority: umi.identity,\n  amount: 1,\n  tokenOwner,\n  tokenStandard: TokenStandard.NonFungible,\n}).sendAndConfirm(umi);",
  "output": "console.log('Minted NFT to:', tokenOwner);",
  "full": "// [IMPORTS]\nimport { mintV1, TokenStandard } from '@metaplex-foundation/mpl-token-metadata';\n// [/IMPORTS]\n\n// [SETUP]\n// Assuming umi is set up with mplTokenMetadata plugin\n// mint from createV1\n\nconst mintPublicKey = mint.publicKey; // From the created mint\nconst tokenOwner = umi.identity.publicKey; // Wallet to receive the token\n// [/SETUP]\n\n// [MAIN]\n// Mint the NFT token\nawait mintV1(umi, {\n  mint: mintPublicKey,\n  authority: umi.identity,\n  amount: 1,\n  tokenOwner,\n  tokenStandard: TokenStandard.NonFungible,\n}).sendAndConfirm(umi);\n// [/MAIN]\n\n// [OUTPUT]\nconsole.log('Minted NFT to:', tokenOwner);\n// [/OUTPUT]\n"
}

const shankSections = {
  "imports": "use mpl_token_metadata::instructions::MintV1CpiBuilder;",
  "setup": "",
  "main": "// 1. every account is specified by a reference to their AccountInfo\n\nlet mint_cpi = MintV1CpiBuilder::new(token_metadata_program_info)\n    .token(token_info)\n    .token_owner(Some(token_owner_info))\n    .metadata(metadata_info)\n    .master_edition(Some(master_edition_info))\n    .mint(mint_info)\n    .payer(payer_info)\n    .authority(update_authority_info)\n    .system_program(system_program_info)\n    .sysvar_instructions(sysvar_instructions_info)\n    .spl_token_program(spl_token_program_info)\n    .spl_ata_program(spl_ata_program_info)\n    .amount(1);\n\nmint_cpi.invoke();",
  "output": "",
  "full": "// [IMPORTS]\nuse mpl_token_metadata::instructions::MintV1CpiBuilder;\n// [/IMPORTS]\n\n// [MAIN]\n// 1. every account is specified by a reference to their AccountInfo\n\nlet mint_cpi = MintV1CpiBuilder::new(token_metadata_program_info)\n    .token(token_info)\n    .token_owner(Some(token_owner_info))\n    .metadata(metadata_info)\n    .master_edition(Some(master_edition_info))\n    .mint(mint_info)\n    .payer(payer_info)\n    .authority(update_authority_info)\n    .system_program(system_program_info)\n    .sysvar_instructions(sysvar_instructions_info)\n    .spl_token_program(spl_token_program_info)\n    .spl_ata_program(spl_ata_program_info)\n    .amount(1);\n\nmint_cpi.invoke();\n// [/MAIN]\n"
}

export const metadata = {
  title: "mint-tokens",
  description: "",
  tags: [],
}

export const examples = {
  kit: {
    framework: 'Kit',
    language: 'javascript',
    code: kitSections.full,
    sections: kitSections,
  },

  umi: {
    framework: 'Umi',
    language: 'javascript',
    code: umiSections.full,
    sections: umiSections,
  },

  shank: {
    framework: 'Shank',
    language: 'rust',
    code: shankSections.full,
    sections: shankSections,
  },

}
