/**
 * Example: create-collection
 *
 * 
 *
 * This file is auto-generated by scripts/build-examples.js
 * Edit the native .js/.ts and .rs files, then run: node scripts/build-examples.js
 */

const kitSections = {
  "imports": "import { generateKeyPairSigner } from '@solana/kit';\nimport { createNft } from '@metaplex-foundation/mpl-token-metadata-kit';",
  "setup": "// Assuming rpc, rpcSubscriptions, sendAndConfirm, and authority are set up\n\nconst collectionMint = await generateKeyPairSigner();",
  "main": "// Create a Collection NFT\nconst [createIx, mintIx] = await createNft({\n  mint: collectionMint,\n  authority,\n  payer: authority,\n  name: 'My Collection',\n  uri: 'https://example.com/my-collection.json',\n  sellerFeeBasisPoints: 550, // 5.5%\n  tokenOwner: authority.address,\n  isCollection: true,\n});\n\nawait sendAndConfirm({\n  instructions: [createIx, mintIx],\n  payer: authority,\n});",
  "output": "console.log('Collection NFT created:', collectionMint.address);",
  "full": "// [IMPORTS]\nimport { generateKeyPairSigner } from '@solana/kit';\nimport { createNft } from '@metaplex-foundation/mpl-token-metadata-kit';\n// [/IMPORTS]\n\n// [SETUP]\n// Assuming rpc, rpcSubscriptions, sendAndConfirm, and authority are set up\n\nconst collectionMint = await generateKeyPairSigner();\n// [/SETUP]\n\n// [MAIN]\n// Create a Collection NFT\nconst [createIx, mintIx] = await createNft({\n  mint: collectionMint,\n  authority,\n  payer: authority,\n  name: 'My Collection',\n  uri: 'https://example.com/my-collection.json',\n  sellerFeeBasisPoints: 550, // 5.5%\n  tokenOwner: authority.address,\n  isCollection: true,\n});\n\nawait sendAndConfirm({\n  instructions: [createIx, mintIx],\n  payer: authority,\n});\n// [/MAIN]\n\n// [OUTPUT]\nconsole.log('Collection NFT created:', collectionMint.address);\n// [/OUTPUT]\n"
}

const umiSections = {
  "imports": "import { generateSigner, percentAmount } from '@metaplex-foundation/umi';\nimport { createNft } from '@metaplex-foundation/mpl-token-metadata';",
  "setup": "// Assuming umi is set up with mplTokenMetadata plugin\n\nconst collectionMint = generateSigner(umi);",
  "main": "await createNft(umi, {\n  mint: collectionMint,\n  name: 'My Collection',\n  uri: 'https://example.com/my-collection.json',\n  sellerFeeBasisPoints: percentAmount(5.5), // 5.5%\n  isCollection: true,\n}).sendAndConfirm(umi);",
  "output": "console.log('Collection NFT created:', collectionMint.publicKey);",
  "full": "// [IMPORTS]\nimport { generateSigner, percentAmount } from '@metaplex-foundation/umi';\nimport { createNft } from '@metaplex-foundation/mpl-token-metadata';\n// [/IMPORTS]\n\n// [SETUP]\n// Assuming umi is set up with mplTokenMetadata plugin\n\nconst collectionMint = generateSigner(umi);\n// [/SETUP]\n\n// [MAIN]\nawait createNft(umi, {\n  mint: collectionMint,\n  name: 'My Collection',\n  uri: 'https://example.com/my-collection.json',\n  sellerFeeBasisPoints: percentAmount(5.5), // 5.5%\n  isCollection: true,\n}).sendAndConfirm(umi);\n// [/MAIN]\n\n// [OUTPUT]\nconsole.log('Collection NFT created:', collectionMint.publicKey);\n// [/OUTPUT]\n"
}

export const metadata = {
  title: "create-collection",
  description: "",
  tags: [],
}

export const examples = {
  kit: {
    framework: 'Kit',
    language: 'javascript',
    code: kitSections.full,
    sections: kitSections,
  },

  umi: {
    framework: 'Umi',
    language: 'javascript',
    code: umiSections.full,
    sections: umiSections,
  },

}
