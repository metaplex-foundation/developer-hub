/**
 * Example: delegate-collection-verify
 *
 * 
 *
 * This file is auto-generated by scripts/build-examples.js
 * Edit the native .js/.ts and .rs files, then run: node scripts/build-examples.js
 */

const kitSections = {
  "imports": "import { address } from '@solana/addresses';\nimport {\n  getVerifyCollectionV1InstructionAsync,\n  findMetadataPda,\n} from '@metaplex-foundation/mpl-token-metadata-kit';",
  "setup": "const mint = address('mintAddress...'); // The item to verify\nconst collectionMint = address('collectionMintAddress...');",
  "main": "const [metadata] = await findMetadataPda({ mint });\nconst verifyIx = await getVerifyCollectionV1InstructionAsync({\n  metadata,\n  collectionMint,\n  authority: collectionDelegate,\n});\n\nawait sendAndConfirm({\n  instructions: [verifyIx],\n  payer: collectionDelegate,\n});",
  "output": "console.log('Collection verified on item');",
  "full": "// [IMPORTS]\nimport { address } from '@solana/addresses';\nimport {\n  getVerifyCollectionV1InstructionAsync,\n  findMetadataPda,\n} from '@metaplex-foundation/mpl-token-metadata-kit';\n// [/IMPORTS]\n\n// [SETUP]\nconst mint = address('mintAddress...'); // The item to verify\nconst collectionMint = address('collectionMintAddress...');\n// [/SETUP]\n\n// [MAIN]\nconst [metadata] = await findMetadataPda({ mint });\nconst verifyIx = await getVerifyCollectionV1InstructionAsync({\n  metadata,\n  collectionMint,\n  authority: collectionDelegate,\n});\n\nawait sendAndConfirm({\n  instructions: [verifyIx],\n  payer: collectionDelegate,\n});\n// [/MAIN]\n\n// [OUTPUT]\nconsole.log('Collection verified on item');\n// [/OUTPUT]\n"
}

const umiSections = {
  "imports": "import { publicKey } from '@metaplex-foundation/umi';\nimport {\n  verifyCollectionV1,\n  findMetadataPda,\n} from '@metaplex-foundation/mpl-token-metadata';",
  "setup": "const mint = publicKey('mintAddress...'); // The item to verify\nconst collectionMint = publicKey('collectionMintAddress...');\nconst collectionDelegate = umi.identity; // The delegate authority",
  "main": "await verifyCollectionV1(umi, {\n  metadata: findMetadataPda(umi, { mint }),\n  collectionMint,\n  authority: collectionDelegate,\n}).sendAndConfirm(umi);",
  "output": "console.log('Collection verified on item');",
  "full": "// [IMPORTS]\nimport { publicKey } from '@metaplex-foundation/umi';\nimport {\n  verifyCollectionV1,\n  findMetadataPda,\n} from '@metaplex-foundation/mpl-token-metadata';\n// [/IMPORTS]\n\n// [SETUP]\nconst mint = publicKey('mintAddress...'); // The item to verify\nconst collectionMint = publicKey('collectionMintAddress...');\nconst collectionDelegate = umi.identity; // The delegate authority\n// [/SETUP]\n\n// [MAIN]\nawait verifyCollectionV1(umi, {\n  metadata: findMetadataPda(umi, { mint }),\n  collectionMint,\n  authority: collectionDelegate,\n}).sendAndConfirm(umi);\n// [/MAIN]\n\n// [OUTPUT]\nconsole.log('Collection verified on item');\n// [/OUTPUT]\n"
}

export const metadata = {
  title: "delegate-collection-verify",
  description: "",
  tags: [],
}

export const examples = {
  kit: {
    framework: 'Kit',
    language: 'javascript',
    code: kitSections.full,
    sections: kitSections,
  },

  umi: {
    framework: 'Umi',
    language: 'javascript',
    code: umiSections.full,
    sections: umiSections,
  },

}
