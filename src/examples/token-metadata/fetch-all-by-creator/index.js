/**
 * Example: fetch-all-by-creator
 *
 * 
 *
 * This file is auto-generated by scripts/build-examples.js
 * Edit the native .js/.ts and .rs files, then run: node scripts/build-examples.js
 */

const umiSections = {
  "imports": "import { publicKey } from '@metaplex-foundation/umi';\nimport { fetchAllDigitalAssetByCreator } from '@metaplex-foundation/mpl-token-metadata';",
  "setup": "// Assuming umi is set up with mplTokenMetadata plugin\n// See getting-started for full setup\n\nconst creator = publicKey('creatorAddress...');",
  "main": "// Assets where the creator is first in the Creator array\nconst assetsA = await fetchAllDigitalAssetByCreator(umi, creator);\n\n// Assets where the creator is second in the Creator array\nconst assetsB = await fetchAllDigitalAssetByCreator(umi, creator, {\n  position: 2,\n});",
  "output": "console.log('Assets with creator at position 1:', assetsA.length);\nassetsA.forEach((asset) => {\n  console.log('Name:', asset.metadata.name);\n});",
  "full": "// [IMPORTS]\nimport { publicKey } from '@metaplex-foundation/umi';\nimport { fetchAllDigitalAssetByCreator } from '@metaplex-foundation/mpl-token-metadata';\n// [/IMPORTS]\n\n// [SETUP]\n// Assuming umi is set up with mplTokenMetadata plugin\n// See getting-started for full setup\n\nconst creator = publicKey('creatorAddress...');\n// [/SETUP]\n\n// [MAIN]\n// Assets where the creator is first in the Creator array\nconst assetsA = await fetchAllDigitalAssetByCreator(umi, creator);\n\n// Assets where the creator is second in the Creator array\nconst assetsB = await fetchAllDigitalAssetByCreator(umi, creator, {\n  position: 2,\n});\n// [/MAIN]\n\n// [OUTPUT]\nconsole.log('Assets with creator at position 1:', assetsA.length);\nassetsA.forEach((asset) => {\n  console.log('Name:', asset.metadata.name);\n});\n// [/OUTPUT]\n"
}

export const metadata = {
  title: "fetch-all-by-creator",
  description: "",
  tags: [],
}

export const examples = {
  umi: {
    framework: 'Umi',
    language: 'javascript',
    code: umiSections.full,
    sections: umiSections,
  },

}
