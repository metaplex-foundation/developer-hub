/**
 * Example: fetch-all-by-owner
 *
 * 
 *
 * This file is auto-generated by scripts/build-examples.js
 * Edit the native .js/.ts and .rs files, then run: node scripts/build-examples.js
 */

const kitSections = {
  "imports": "import { fetchAllDigitalAsset } from '@metaplex-foundation/mpl-token-metadata-kit';",
  "setup": "// Assuming rpc is set up\n// See getting-started for full setup\n\n// Get mint addresses from token accounts (use DAS API or getProgramAccounts)\nconst mintAddresses = ['mint1...', 'mint2...', 'mint3...'];",
  "main": "// Fetch multiple digital assets efficiently in a single batch\nconst assets = await fetchAllDigitalAsset(rpc, mintAddresses);",
  "output": "assets.forEach((asset) => {\n  console.log('Name:', asset.metadata.name);\n  console.log('URI:', asset.metadata.uri);\n});",
  "full": "// [IMPORTS]\nimport { fetchAllDigitalAsset } from '@metaplex-foundation/mpl-token-metadata-kit';\n// [/IMPORTS]\n\n// [SETUP]\n// Assuming rpc is set up\n// See getting-started for full setup\n\n// Get mint addresses from token accounts (use DAS API or getProgramAccounts)\nconst mintAddresses = ['mint1...', 'mint2...', 'mint3...'];\n// [/SETUP]\n\n// [MAIN]\n// Fetch multiple digital assets efficiently in a single batch\nconst assets = await fetchAllDigitalAsset(rpc, mintAddresses);\n// [/MAIN]\n\n// [OUTPUT]\nassets.forEach((asset) => {\n  console.log('Name:', asset.metadata.name);\n  console.log('URI:', asset.metadata.uri);\n});\n// [/OUTPUT]\n"
}

const umiSections = {
  "imports": "import { publicKey } from '@metaplex-foundation/umi';\nimport { fetchAllDigitalAssetByOwner } from '@metaplex-foundation/mpl-token-metadata';",
  "setup": "// Assuming umi is set up with mplTokenMetadata plugin\n// See getting-started for full setup\n\nconst owner = publicKey('ownerAddress...');",
  "main": "// Fetch all digital assets owned by a wallet\nconst assets = await fetchAllDigitalAssetByOwner(umi, owner);",
  "output": "assets.forEach((asset) => {\n  console.log('Name:', asset.metadata.name);\n  console.log('URI:', asset.metadata.uri);\n});",
  "full": "// [IMPORTS]\nimport { publicKey } from '@metaplex-foundation/umi';\nimport { fetchAllDigitalAssetByOwner } from '@metaplex-foundation/mpl-token-metadata';\n// [/IMPORTS]\n\n// [SETUP]\n// Assuming umi is set up with mplTokenMetadata plugin\n// See getting-started for full setup\n\nconst owner = publicKey('ownerAddress...');\n// [/SETUP]\n\n// [MAIN]\n// Fetch all digital assets owned by a wallet\nconst assets = await fetchAllDigitalAssetByOwner(umi, owner);\n// [/MAIN]\n\n// [OUTPUT]\nassets.forEach((asset) => {\n  console.log('Name:', asset.metadata.name);\n  console.log('URI:', asset.metadata.uri);\n});\n// [/OUTPUT]\n"
}

export const metadata = {
  title: "fetch-all-by-owner",
  description: "",
  tags: [],
}

export const examples = {
  kit: {
    framework: 'Kit',
    language: 'javascript',
    code: kitSections.full,
    sections: kitSections,
  },

  umi: {
    framework: 'Umi',
    language: 'javascript',
    code: umiSections.full,
    sections: umiSections,
  },

}
