/**
 * Example: fetch-all-with-token-by-owner
 *
 * 
 *
 * This file is auto-generated by scripts/build-examples.js
 * Edit the native .js/.ts and .rs files, then run: node scripts/build-examples.js
 */

const umiSections = {
  "imports": "import { publicKey } from '@metaplex-foundation/umi';\nimport { fetchAllDigitalAssetWithTokenByOwner } from '@metaplex-foundation/mpl-token-metadata';",
  "setup": "// Assuming umi is set up with mplTokenMetadata plugin\n// See getting-started for full setup\n\nconst owner = publicKey('ownerAddress...');",
  "main": "// Fetch all digital assets with their token accounts by owner\nconst assets = await fetchAllDigitalAssetWithTokenByOwner(umi, owner);",
  "output": "console.log('Found', assets.length, 'assets');\nassets.forEach((asset) => {\n  console.log('Name:', asset.metadata.name);\n  console.log('Token Amount:', asset.token.amount);\n});",
  "full": "// [IMPORTS]\nimport { publicKey } from '@metaplex-foundation/umi';\nimport { fetchAllDigitalAssetWithTokenByOwner } from '@metaplex-foundation/mpl-token-metadata';\n// [/IMPORTS]\n\n// [SETUP]\n// Assuming umi is set up with mplTokenMetadata plugin\n// See getting-started for full setup\n\nconst owner = publicKey('ownerAddress...');\n// [/SETUP]\n\n// [MAIN]\n// Fetch all digital assets with their token accounts by owner\nconst assets = await fetchAllDigitalAssetWithTokenByOwner(umi, owner);\n// [/MAIN]\n\n// [OUTPUT]\nconsole.log('Found', assets.length, 'assets');\nassets.forEach((asset) => {\n  console.log('Name:', asset.metadata.name);\n  console.log('Token Amount:', asset.token.amount);\n});\n// [/OUTPUT]\n"
}

export const metadata = {
  title: "fetch-all-with-token-by-owner",
  description: "",
  tags: [],
}

export const examples = {
  umi: {
    framework: 'Umi',
    language: 'javascript',
    code: umiSections.full,
    sections: umiSections,
  },

}
