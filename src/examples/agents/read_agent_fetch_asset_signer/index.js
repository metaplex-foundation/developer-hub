/**
 * Example: read_agent_fetch_asset_signer
 *
 * 
 *
 * This file is auto-generated by scripts/build-examples.js
 * Edit the native .js/.ts and .rs files, then run: node scripts/build-examples.js
 */

const umiSections = {
  "imports": "import { findAssetSignerPda, mplCore } from '@metaplex-foundation/mpl-core'\nimport { createUmi } from '@metaplex-foundation/umi-bundle-defaults'\nimport { publicKey } from '@metaplex-foundation/umi'",
  "setup": "const umi = createUmi('https://api.mainnet-beta.solana.com').use(mplCore())\nconst assetPublicKey = publicKey('AGENT_CORE_ASSET_ADDRESS')",
  "main": "const assetSignerPda = findAssetSignerPda(umi, { asset: assetPublicKey })\nconst balance = await umi.rpc.getBalance(assetSignerPda)\n\nconsole.log('Agent wallet:', assetSignerPda)\nconsole.log('Balance:', balance.basisPoints.toString(), 'lamports')",
  "output": "// Agent wallet: 6ttUwc5VVmHeVKTddB6XM5vQgBMfw62DThuoiVEufVZq\n// Balance: 1000000 lamports",
  "full": "// [IMPORTS]\nimport { findAssetSignerPda, mplCore } from '@metaplex-foundation/mpl-core'\nimport { createUmi } from '@metaplex-foundation/umi-bundle-defaults'\nimport { publicKey } from '@metaplex-foundation/umi'\n// [/IMPORTS]\n\n// [SETUP]\nconst umi = createUmi('https://api.mainnet-beta.solana.com').use(mplCore())\nconst assetPublicKey = publicKey('AGENT_CORE_ASSET_ADDRESS')\n// [/SETUP]\n\n// [MAIN]\nconst assetSignerPda = findAssetSignerPda(umi, { asset: assetPublicKey })\nconst balance = await umi.rpc.getBalance(assetSignerPda)\n\nconsole.log('Agent wallet:', assetSignerPda)\nconsole.log('Balance:', balance.basisPoints.toString(), 'lamports')\n// [/MAIN]\n\n// [OUTPUT]\n// Agent wallet: 6ttUwc5VVmHeVKTddB6XM5vQgBMfw62DThuoiVEufVZq\n// Balance: 1000000 lamports\n// [/OUTPUT]\n"
}

export const metadata = {
  title: "read_agent_fetch_asset_signer",
  description: "",
  tags: [],
}

export const examples = {
  umi: {
    framework: 'Umi',
    language: 'typescript',
    code: umiSections.full,
    sections: umiSections,
  },

}
