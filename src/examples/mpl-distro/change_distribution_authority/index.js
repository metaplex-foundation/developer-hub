/**
 * Example: change_distribution_authority
 *
 * 
 *
 * This file is auto-generated by scripts/build-examples.js
 * Edit the native .js/.ts and .rs files, then run: node scripts/build-examples.js
 */

const umiSections = {
  "imports": "import { mplDistro, updateDistribution } from '@metaplex-foundation/mpl-distro'\nimport { publicKey } from '@metaplex-foundation/umi'\nimport { createUmi } from '@metaplex-foundation/umi-bundle-defaults'",
  "setup": "const umi = createUmi(\n  process.env.RPC_URL ?? 'https://api.devnet.solana.com'\n).use(mplDistro())\n\nconst distribution = publicKey(process.env.DISTRIBUTION_ADDRESS!)\nconst currentAuthority = umi.identity\nconst nextAuthority = publicKey(process.env.NEXT_AUTHORITY!)",
  "main": "await updateDistribution(umi, {\n  distribution,\n  authority: currentAuthority,\n  newAuthority: nextAuthority,\n}).sendAndConfirm(umi)",
  "output": "// Subsequent deposits, withdrawals, and updates require the new authority.",
  "full": "// [IMPORTS]\nimport { mplDistro, updateDistribution } from '@metaplex-foundation/mpl-distro'\nimport { publicKey } from '@metaplex-foundation/umi'\nimport { createUmi } from '@metaplex-foundation/umi-bundle-defaults'\n// [/IMPORTS]\n\n// [SETUP]\nconst umi = createUmi(\n  process.env.RPC_URL ?? 'https://api.devnet.solana.com'\n).use(mplDistro())\n\nconst distribution = publicKey(process.env.DISTRIBUTION_ADDRESS!)\nconst currentAuthority = umi.identity\nconst nextAuthority = publicKey(process.env.NEXT_AUTHORITY!)\n// [/SETUP]\n\n// [MAIN]\nawait updateDistribution(umi, {\n  distribution,\n  authority: currentAuthority,\n  newAuthority: nextAuthority,\n}).sendAndConfirm(umi)\n// [/MAIN]\n\n// [OUTPUT]\n// Subsequent deposits, withdrawals, and updates require the new authority.\n// [/OUTPUT]\n"
}

export const metadata = {
  title: "change_distribution_authority",
  description: "",
  tags: [],
}

export const examples = {
  umi: {
    framework: 'Umi',
    language: 'typescript',
    code: umiSections.full,
    sections: umiSections,
  },

}
