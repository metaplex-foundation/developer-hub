/**
 * Example: calculate_deposit
 *
 * 
 *
 * This file is auto-generated by scripts/build-examples.js
 * Edit the native .js/.ts and .rs files, then run: node scripts/build-examples.js
 */

const umiSections = {
  "imports": "import { publicKey } from '@metaplex-foundation/umi'",
  "setup": "const allocations = [\n  { address: publicKey(process.env.RECIPIENT_1!), amount: 100_000n },\n  { address: publicKey(process.env.RECIPIENT_2!), amount: 250_000n },\n]",
  "main": "const totalAmount = allocations.reduce(\n  (total, allocation) => total + BigInt(allocation.amount),\n  0n\n)\nconsole.log(totalAmount)",
  "output": "// 350000",
  "full": "// [IMPORTS]\nimport { publicKey } from '@metaplex-foundation/umi'\n// [/IMPORTS]\n\n// [SETUP]\nconst allocations = [\n  { address: publicKey(process.env.RECIPIENT_1!), amount: 100_000n },\n  { address: publicKey(process.env.RECIPIENT_2!), amount: 250_000n },\n]\n// [/SETUP]\n\n// [MAIN]\nconst totalAmount = allocations.reduce(\n  (total, allocation) => total + BigInt(allocation.amount),\n  0n\n)\nconsole.log(totalAmount)\n// [/MAIN]\n\n// [OUTPUT]\n// 350000\n// [/OUTPUT]\n"
}

export const metadata = {
  title: "calculate_deposit",
  description: "",
  tags: [],
}

export const examples = {
  umi: {
    framework: 'Umi',
    language: 'typescript',
    code: umiSections.full,
    sections: umiSections,
  },

}
