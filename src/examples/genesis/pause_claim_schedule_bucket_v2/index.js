/**
 * Example: pause_claim_schedule_bucket_v2
 *
 * 
 *
 * This file is auto-generated by scripts/build-examples.js
 * Edit the native .js/.ts and .rs files, then run: node scripts/build-examples.js
 */

const umiSections = {
  "imports": "import {\n  genesis,\n  setClaimSchedulePausedStateV2,\n} from '@metaplex-foundation/genesis'\nimport { keypairIdentity, publicKey } from '@metaplex-foundation/umi'\nimport { createUmi } from '@metaplex-foundation/umi-bundle-defaults'",
  "setup": "const umi = createUmi('https://api.mainnet-beta.solana.com').use(genesis())\n\n// umi.use(keypairIdentity(yourKeypair))\n\nconst genesisAccount = publicKey('YOUR_GENESIS_ACCOUNT')\nconst vestingBucket = publicKey('CLAIM_SCHEDULE_BUCKET_PDA')",
  "main": "await setClaimSchedulePausedStateV2(umi, {\n  genesisAccount,\n  bucket: vestingBucket,\n  signer: umi.identity,\n  paused: true,\n  padding: Array(6).fill(0),\n}).sendAndConfirm(umi)",
  "output": "// Set paused to false and send again to resume vesting.",
  "full": "// [IMPORTS]\nimport {\n  genesis,\n  setClaimSchedulePausedStateV2,\n} from '@metaplex-foundation/genesis'\nimport { keypairIdentity, publicKey } from '@metaplex-foundation/umi'\nimport { createUmi } from '@metaplex-foundation/umi-bundle-defaults'\n// [/IMPORTS]\n\n// [SETUP]\nconst umi = createUmi('https://api.mainnet-beta.solana.com').use(genesis())\n\n// umi.use(keypairIdentity(yourKeypair))\n\nconst genesisAccount = publicKey('YOUR_GENESIS_ACCOUNT')\nconst vestingBucket = publicKey('CLAIM_SCHEDULE_BUCKET_PDA')\n// [/SETUP]\n\n// [MAIN]\nawait setClaimSchedulePausedStateV2(umi, {\n  genesisAccount,\n  bucket: vestingBucket,\n  signer: umi.identity,\n  paused: true,\n  padding: Array(6).fill(0),\n}).sendAndConfirm(umi)\n// [/MAIN]\n\n// [OUTPUT]\n// Set paused to false and send again to resume vesting.\n// [/OUTPUT]\n"
}

export const metadata = {
  title: "pause_claim_schedule_bucket_v2",
  description: "",
  tags: [],
}

export const examples = {
  umi: {
    framework: 'Umi',
    language: 'typescript',
    code: umiSections.full,
    sections: umiSections,
  },

}
