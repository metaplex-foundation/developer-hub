/**
 * Example: add-to-collection
 *
 * 
 *
 * This file is auto-generated by scripts/build-examples.js
 * Edit the native .js/.ts and .rs files, then run: node scripts/build-examples.js
 */

const umiSections = {
  "imports": "import { publicKey } from '@metaplex-foundation/umi'\nimport {\n  update,\n  fetchAsset,\n  fetchCollection,\n  updateAuthority,\n} from '@metaplex-foundation/mpl-core'",
  "setup": "const assetId = publicKey('11111111111111111111111111111111')\nconst collectionId = publicKey('22222222222222222222222222222222')",
  "main": "const asset = await fetchAsset(umi, assetId)\nconst collection = await fetchCollection(umi, collectionId)\n\nawait update(umi, {\n  asset,\n  newCollection: collection.publicKey,\n  newUpdateAuthority: updateAuthority('Collection', [collection.publicKey]),\n}).sendAndConfirm(umi)",
  "output": "console.log('Asset added to collection')",
  "full": "// [IMPORTS]\nimport { publicKey } from '@metaplex-foundation/umi'\nimport {\n  update,\n  fetchAsset,\n  fetchCollection,\n  updateAuthority,\n} from '@metaplex-foundation/mpl-core'\n// [/IMPORTS]\n\n// [SETUP]\nconst assetId = publicKey('11111111111111111111111111111111')\nconst collectionId = publicKey('22222222222222222222222222222222')\n// [/SETUP]\n\n// [MAIN]\nconst asset = await fetchAsset(umi, assetId)\nconst collection = await fetchCollection(umi, collectionId)\n\nawait update(umi, {\n  asset,\n  newCollection: collection.publicKey,\n  newUpdateAuthority: updateAuthority('Collection', [collection.publicKey]),\n}).sendAndConfirm(umi)\n// [/MAIN]\n\n// [OUTPUT]\nconsole.log('Asset added to collection')\n// [/OUTPUT]\n"
}

const cliSections = {
  "imports": "",
  "setup": "",
  "main": "",
  "output": "",
  "full": "# Add a standalone asset to a collection\nmplx core asset update <assetId> --collection <collectionId>\n\n# Or use the convenience alias\nmplx core collection add <collectionId> <assetId>\n"
}

export const metadata = {
  title: "add-to-collection",
  description: "",
  tags: [],
}

export const examples = {
  umi: {
    framework: 'Umi',
    language: 'javascript',
    code: umiSections.full,
    sections: umiSections,
  },

  cli: {
    framework: 'CLI',
    language: 'bash',
    code: cliSections.full,
    sections: cliSections,
  },

}
